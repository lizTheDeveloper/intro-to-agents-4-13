# Company news + pgvector (2026-04-13)

- Added `intel_company_news_article` and `intel_company_news_chunk` with `vector(1024)` and HNSW index.
- `mlx_qwen_embedder.py` loads MLX community Qwen3 0.6B-class weights; mean pool + L2 normalize.
- `news_ingestion_agent.py` uses Tavily search (topic `news`) + extract, chunks text, embeds with MLX, writes to Postgres.
- Tests: `tests/test_news_vector_store.py` (pg + random unit vectors, no MLX download).
