# Hiring intel SQLite (2026-04-13)

- Added normalized tables for bundles, target profiles, companies, funding rounds, leads, signals, claims, citations, executive motions, and interview prep.
- Exposed sixteen `intel_*` tools so the agent writes through structured operations instead of raw SQL.
- Default DB file `hiring_intel.db`; override with `HIRING_INTEL_DB_PATH`.
