# Hiring intel → PostgreSQL (2026-04-13)

- Replaced SQLite with PostgreSQL via `psycopg` and `DATABASE_URL`.
- Canonical DDL lives in `sql/hiring_intel_schema.sql` (Postgres types, `EXCLUDED` upserts, `RETURNING` for ids).
- Integration tests require `DATABASE_URL` and load `.env` via `python-dotenv`.
