# Intel analyst agent (2026-04-13)

- `hiring_intel_store.bundle_get_detail` returns bundle + target profile rows.
- `news_vector_store.news_recent_articles` lists latest articles when semantic search is skipped or unavailable.
- `intel_analyst_gather.py` merges `leads_list` + `lead_get_full` with per-company news (semantic via MLX or recency fallback).
- `intel_analyst_agent.py` compacts JSON for context and synthesizes an executive brief via Groq (`INTEL_ANALYST_MODEL`).
