# Schema change queue (2026-04-13)

- New table `intel_schema_change_queue` (Postgres) plus tools: submit, list, export-pending.
- `export_pending_schema_requests()` writes markdown under `INTEL_SCHEMA_QUEUE_MD` (default `plans/schema_change_queue.md`) and sets status to `exported`.
- `run_intel_pipeline.py` calls export after analyst; standalone `process_intel_schema_queue.py` for manual runs.
